import FormValues from "../FormValues";
import ValuesList from "../ValuesList";

const MainContainer = () => {
  return (
    <div>
      <FormValues />
      <ValuesList />
    </div>
  );
};

export default MainContainer;
